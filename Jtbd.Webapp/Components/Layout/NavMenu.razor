@using Jtbd.Webapp.Service
@rendermode InteractiveServer

<div class="top-row ps-3 navbar navbar-dark">
    <div class="container-fluid align-content-center">
        <a class="navbar-brand" href="">
            <img src="logoinnova.png" />
        </a>
    </div>
</div>

<input type="checkbox" title="Navigation menu" class="navbar-toggler" />

<div class="nav-scrollable" onclick="document.querySelector('.navbar-toggler').click()">
    <nav class="nav flex-column">
        <div class="nav-item px-3">
            <NavLink class="nav-link" href="" Match="NavLinkMatch.All">
                <p><i class="bi bi-house-door m-1"></i> Home</p>
            </NavLink>
        </div>

        <div class="nav-item px-3">
            <NavLink class="nav-link" href="proyect/proyectos">
                <p><i class="bi bi-clipboard-data m-1"></i> Proyectos</p>
            </NavLink>
        </div>

        @* @if (sessionService.UserRol == 1)
        { *@
            <div class="nav-item px-3">
                <NavLink class="nav-link" href="#" @onclick="() => isExpandSubNav = !isExpandSubNav" @onmousemove="() => isExpandSubNav = !isExpandSubNav">
                    <p><i class="bi bi-list-nested m-1"></i> Admin</p>
                </NavLink>

                @if (isExpandSubNav)
                {
                    <ul class="nav flex-column submenu">
                        <li class="nav-item px-3">
                            <NavLink class="nav-link" href="admin/categorias" >
                                <p><i class="bi bi-bezier2 m-1"></i> Categorias</p>
                            </NavLink>
                        </li>
                        <li class="nav-item px-3">
                            <NavLink class="nav-link" href="admin/departamentos" >
                                <p><i class="bi bi-bezier m-1"></i> Departamentos</p>
                            </NavLink>
                        </li>
                        <li class="nav-item px-3">
                            <NavLink class="nav-link" href="admin/usuarios" >
                                <p><i class="bi bi-person m-1"></i> Usuarios</p>
                            </NavLink>
                        </li>
                    </ul>
                }
            </div>
        @* } *@
    </nav>
</div>

@code {

    private bool isExpandSubNavP = false;
    private bool isExpandSubNav = false;
    // @inject SessionService sessionService
}